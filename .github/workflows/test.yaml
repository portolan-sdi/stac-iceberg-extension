name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    # npm test already runs remark, so the reusable workflow does not repeat it.
    uses: portolan-sdi/portolan-ops/.github/workflows/reusable-stac-ext.yml@v1
    with:
      run-remark: false
